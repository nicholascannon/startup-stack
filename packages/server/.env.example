RELEASE=local
PORT=8000
NODE_ENV=development
CORS_HOSTS=http://localhost:5173
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/startup-stack
# Optional: tune pool size (default max=10, min=2). With 5 Fly machines, 10 each = 50 connections to PgBouncer.
# DATABASE_POOL_MAX=10
# DATABASE_POOL_MIN=2
